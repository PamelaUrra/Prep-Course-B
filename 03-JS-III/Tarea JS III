* Arrays: son un tipo de estructura de datos que consiste en una lista de elementos de cualquier tipo, separados por coma entre si. los arreglos se ubican dentro de corchetes.
Ejemplo: var arreglo =[1,-2,ele,true,null,x];
* `==` vs. `===`